<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="ToolTip" x:Key="PlainToolTip">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate>
                    <Border BorderBrush="{DynamicResource ColorBrush1}" BorderThickness="1.5" CornerRadius="4"
                            MaxWidth="500" Background="{DynamicResource ColorBrushToolTip}" Margin="0,0,4,4"
                            SnapsToDevicePixels="True">
                        <Border.Effect>
                            <DropShadowEffect Opacity="0.3" BlurRadius="4" ShadowDepth="2"
                                              Color="{DynamicResource ColorObject1}" />
                        </Border.Effect>
                        <TextBlock Text="{TemplateBinding ContentControl.Content}" TextWrapping="Wrap"
                                   Foreground="{DynamicResource ColorBrush1}" Margin="7,5" FontSize="12" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="ToolTip" BasedOn="{StaticResource PlainToolTip}" />
    <Style x:Key="ExtendTextToolTip" TargetType="ToolTip">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate>
                    <Border BorderBrush="{DynamicResource ColorBrush1}" BorderThickness="1"
                            Background="{DynamicResource ColorBrushToolTip}" CornerRadius="3"
                            MaxWidth="500" Margin="0,0,4,4" SnapsToDevicePixels="True">
                        <Border.Effect>
                            <DropShadowEffect Opacity="0.3" BlurRadius="4" ShadowDepth="2"
                                              Color="{DynamicResource ColorObject1}" />
                        </Border.Effect>
                        <TextBlock Text="{TemplateBinding ContentControl.Content}" TextWrapping="Wrap"
                                   Foreground="{DynamicResource ColorBrush1}" Margin="7,5" FontSize="12" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
