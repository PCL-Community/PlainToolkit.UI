<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- SLIDER — PCL-CE MySlider style with proper Track/Thumb -->
    <Style TargetType="Slider" x:Key="PlainSlider">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="MinHeight" Value="24" />
        <Setter Property="MinWidth" Value="40" />
        <Setter Property="SnapsToDevicePixels" Value="False" />
        <Setter Property="UseLayoutRounding" Value="False" />
        <Setter Property="BorderBrush" Value="{DynamicResource ColorBrushBg0}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Slider">
                    <Grid MinHeight="24" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                        <!-- 3 rows: thumb track centered vertically -->
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="14" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <!-- Track background line (centered in row) -->
                        <Border Grid.Row="1" x:Name="trackBackground"
                                Height="4" Margin="6,0"
                                VerticalAlignment="Center"
                                CornerRadius="2"
                                Background="{DynamicResource ColorBrush5}"
                                Opacity="0.4" />

                        <!-- Interactive Track (expands to fill row = 14px) -->
                        <Track x:Name="PART_Track" Grid.Row="1" Margin="6,0">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton x:Name="DecreaseButton"
                                              Command="{x:Static Slider.DecreaseLarge}">
                                    <RepeatButton.Template>
                                        <ControlTemplate TargetType="RepeatButton">
                                            <Border Height="4" VerticalAlignment="Center"
                                                    Background="{Binding BorderBrush, RelativeSource={RelativeSource AncestorType={x:Type Slider}}}" 
                                                    CornerRadius="2,0,0,2" />
                                        </ControlTemplate>
                                    </RepeatButton.Template>
                                </RepeatButton>
                            </Track.DecreaseRepeatButton>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="{x:Static Slider.IncreaseLarge}">
                                    <RepeatButton.Template>
                                        <ControlTemplate TargetType="RepeatButton">
                                            <Border Background="Transparent" />
                                        </ControlTemplate>
                                    </RepeatButton.Template>
                                </RepeatButton>
                            </Track.IncreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb x:Name="ShapeDotThumb"
                                       Width="14" Height="14"
                                       RenderTransformOrigin="0.5,0.5"
                                       Background="Transparent">
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Grid>
                                                <Ellipse x:Name="ShapeDot"
                                                         Width="14" Height="14"
                                                         Stroke="{Binding BorderBrush, RelativeSource={RelativeSource AncestorType={x:Type Slider}}}"
                                                         StrokeThickness="1.5"
                                                         Fill="{DynamicResource ColorBrushWhite}"
                                                         RenderTransformOrigin="0.5,0.5" />
                                            </Grid>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="trackBackground" Property="Background" Value="{DynamicResource ColorBrushGray5}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="Slider" BasedOn="{StaticResource PlainSlider}" />

</ResourceDictionary>
